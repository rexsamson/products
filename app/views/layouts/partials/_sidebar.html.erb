			<aside class="main-sidebar">
				<!-- sidebar: style can be found in sidebar.less -->
				<section class="sidebar">
					<!-- Sidebar user panel (optional) -->
					<div class="user-panel">
						<div class="pull-left image">
							<h4><i class="fa fa-fw fa-user-circle" style="color:white;"></i></h4>
						</div>
						<div class="pull-left info">
							<p><%= render html: current_user.fullname.titleize rescue '-' %></p><!-- Status -->
							<a><i class="fa fa-circle text-success"></i> Online</a>
						</div>
					</div><!-- search form (Optional) -->
					<!-- Sidebar Menu -->
					<ul class="sidebar-menu" id="menu" style="font-size:14px !important">
						
						
						<li class="<%= 'active' if params[:controller] == 'products' %>">
									<%= link_to "<i class='fa fa-cube text-white'></i> <span>Products</span>".html_safe, products_path, :rel => "tab" %>
						</li>
						
						
						
					</ul><!-- /.sidebar-menu -->
				</section><!-- /.sidebar -->
			</aside>